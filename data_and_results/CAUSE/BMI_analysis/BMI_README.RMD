###BMI data for reproducing CAUSE results

With the data in each of the folders you can obtain the results that are published by running the run_cause.R in CAUSE/CODE. 

Importantly, due to the large size of the file with the merged GWAS summary statistics between BMI and the physical activity/inactivity traits, run_cause.R might re-run the last step with X=res$data. **If you want to use the original merged dataframe you need to obtain it through this zenodo link:** (add. )
